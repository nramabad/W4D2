<%= render 'form', cat: @cat, action: :edit %>

<!-- 
<form action="<%=cat_url(@cat)%>" method="post">
  <input type="hidden" name="_method" value="patch">
  <label>
    Name: <input type="text" name="cats[name]" value="<%= @cat.name %>" >
  </label>
  <br>
  <label>
    Sex:
    <input type="radio" <%= "checked" if @cat.sex == "M"%> name="cats[sex]" value="M" > Male
    <input type="radio" <%= "checked" if @cat.sex == "F"%> name="cats[sex]" value="F" > Female
  </label>
  <br>
  <label>
    Color:
    <select name="cats[color]">
      <option disabled <%="selected" unless Cat.colors.include?(@cat.color) %> >Please Select Color</option>
      <% Cat.colors.each do |color| %>
        <option value="<%=color%>" <%="selected" if @cat.color == color%> ><%=color%></option>
      <% end %>
    </select>
  </label>
  <br>
  <label>
    Birth Date: <input type="date" name="cats[birth_date]" value="<%= @cat.birth_date %>" >
  </label>
  <br>
  <label>
    Description: <input type="textarea" name="cats[description]" value="<%= @cat.description %>" >
  </label>
  <br>
  <input type="submit" value="Edit Kitteh">
</form> -->
